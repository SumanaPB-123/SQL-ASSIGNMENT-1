1. SELECT  ENAME , JOB 
   FROM EMP
   WHERE JOB= "CLERK"

2. SELECT  ENAME , JOB 
   FROM EMP
   WHERE JOB= "CLERK" or  JOB="MANAGER";

3. SELECT GETDATE();

4. SELECT ENAME ,SAL
   FROM EMP
   WHERE SAL BETWEEN 500 AND 1000;

5.SELECT ENAME ,HIREDATE
 FROM EMP
 WHERE HIREDATE LIKE ' __-12- % '
 
6.SELECT ENAME ,SAL
   FROM EMP
   WHERE SAL NOT BETWEEN 1000 AND 2500;

7.SELECT ENAME ,SAL
   FROM EMP
   WHERE SAL  IN (800,950, 3000,5000);

8. SELECT ENAME 
   FROM EMP
   WHERE ENAME LIKE '_L%';

9. SELECT ENAME 
   FROM EMP
   WHERE ENAME LIKE '%D';

10.SELECT ENAME 
   FROM EMP
   WHERE  len(ENAME)=5;

11. SELECT ENAME 
    FROM EMP
   WHERE ENAME LIKE '%T_R%';

12. SELECT ENAME 
    FROM EMP
    WHERE ENAME LIKE '%A%';

13. SELECT ENAME , HIREDATE 
    FROM EMP 
    WHERE DATEPART(dd,HIREDATE)<=19;

14. SELECT ENAME , EMPNO
    FROM EMP 
    WHERE ENAME='JAMES';

15.SELECT ENAME , SAL
   FROM EMP 
   WHERE ENAME='KING';
 
16.SELECT ENAME , JOB
   FROM EMP 
   WHERE JOB='PRESIDENT';

17.SELECT *
   FROM EMP
   WHERE JOB='MANAGER';

18. SELECT * 
    FROM EMP 
    WHERE COMM IS NOT NULL;

19. SELECT ENAME , HIREDATE
    FROM EMP 
    WHERE HIREDATE < ('1981-04-01');

20. SELECT  ENAME , SAL
    FROM EMP
    WHERE ENAME ='KING' or  ENAME ='BLAKE' or   ENAME ='FORD' or   ENAME ='SMITH';

21. SELECT  ENAME , SAL
    FROM EMP
    WHERE SAL<3500;

22. SELECT *
    FROM EMP 
    WHERE JOB='CLERK'; 

23. SELECT ENAME , (12*SAL) AS ANNUAL_SAL
    FROM EMP  

24. SELECT ENAME , (SAL+(0.2*SAL))*12 AS ANNUAL_SAL
    FROM EMP
    WHERE ENAME='FORD';

25. SELECT DEPTNO, SUM(SAL) AS TOTAL_SAL
    FROM EMP
    GROUP BY DEPTNO; 

26. SELECT ename, DEPTNO,  MIN(SAL) AS SAL
    FROM EMP 
    WHERE DEPTNO=10;

27. SELECT  JOB, ROUND(AVG(SAL),2) AS  AVG_SAL
    FROM EMP
   WHERE JOB='MANAGER';

28.  SELECT  JOB, SUM(SAL) AS  TOTAL_SAL
    FROM EMP
   WHERE JOB='SALESMAN';

29. SELECT  DEPTNO, (ROUND(AVG(SAL),2))  
    FROM EMP
  GROUP BY DEPTNO
  HAVING AVG(SAL)<
        (SELECT AVG(SAL)
		FROM EMP);

30. SELECT  JOB, COUNT(JOB) AS EMPLOYEE_COUNT
    FROM EMP
    GROUP BY JOB;

31.SELECT  JOB, MAX(SAL) AS MAXIMUM_SALARY
   FROM EMP
   WHERE JOB='ANALYST';

32.SELECT  JOB, MAX(COMM) AS HIGHEST_COMMISSION
   FROM EMP

33.SELECT COUNT(DISTINCT DEPTNO) AS DEPARTMENT_COUNT
   FROM EMP

34. SELECT ENAME 
   FROM EMP
   WHERE ENAME LIKE '__L%';

